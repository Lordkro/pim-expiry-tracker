# Azure Functions profile.ps1
#
# This profile runs once per PowerShell worker instance on cold start.
# Use it for one-time setup, module imports, or environment configuration.
#
# No additional setup needed — Microsoft Graph connection is handled in run.ps1.
